defaults: &defaults
  host: 127.0.0.1
  port: 27017

development:
  <<: *defaults
  database: rails_starter_development

test:
  <<: *defaults
  database: rails_starter_test

# set these environment variables on your prod server
production:
  <<: *defaults
  database: rails_starter